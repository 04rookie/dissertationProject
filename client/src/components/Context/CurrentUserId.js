import React, { createContext } from "react";
const CurrentUserId = createContext();
//export const CurrentUserIdProvider = CurrentUserId.Provider;
//export const CurrentUserIdConsumer = CurrentUserId.Consumer;
export default CurrentUserId;